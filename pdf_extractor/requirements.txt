# =============================================================================
# PDF Extractor — Python dependencies
# =============================================================================
# Install everything:   pip install -r requirements.txt
# Minimal (no OCR/heavy):  pip install -r requirements.txt --no-deps  ← not recommended
#
# Optional extras are commented out.  Uncomment as needed.
# =============================================================================

# ── Transport layer ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # multipart/form-data uploads

# ── CLI ───────────────────────────────────────────────────────────────────────
click>=8.1.0
rich>=13.0.0

# ── Core PDF parsing ──────────────────────────────────────────────────────────
pymupdf>=1.23.0                  # text extraction, page rendering, image export
pdfplumber>=0.9.0                # table detection + text with coordinates
pypdf>=3.0.0                     # preflight: metadata, encryption check, page count
pdfminer.six>=20221105           # character-level text with bboxes

# ── Tables ────────────────────────────────────────────────────────────────────
tabulate>=0.9.0                  # Markdown table rendering

# ── OCR ───────────────────────────────────────────────────────────────────────
pytesseract>=0.3.10              # Tesseract wrapper (requires tesseract-ocr system pkg)
pdf2image>=1.16.0                # PDF page → PIL Image (requires poppler)
easyocr>=1.7.0                   # DNN-based OCR, no system binary needed
rapidocr-onnxruntime>=1.3.0      # Lightweight ONNX OCR, CPU-friendly
img2table>=1.2.0                 # Scanned table extraction via OCR

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=10.0.0
opencv-python-headless>=4.8.0    # Scanned-page preprocessing (deskew, binarize)

# ── Language detection ────────────────────────────────────────────────────────
langdetect>=1.0.9
chardet>=5.0.0

# ── Post-processing ───────────────────────────────────────────────────────────
pyspellchecker>=0.7.0            # OCR artifact spell correction

# ── Progress ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0

# ── Advanced backends (system deps already present in Dockerfile) ────────────
markitdown>=0.0.1                # Microsoft MarkItDown — broad format support
camelot-py[cv]>=0.11.0           # Camelot — high-accuracy table extraction (uses ghostscript)
tabula-py>=2.7.0                 # Tabula — Java-based table extraction (uses JRE)
tika>=1.24                       # Apache Tika — 1000+ formats (downloads ~60MB JAR on first use)

# ── Optional: extra-heavy ML backends (uncomment if you need them) ───────────
# docling>=2.0.0                 # IBM Docling — pulls in PyTorch (~1GB image bloat)
